#include <bits/stdc++.h> 
int squareRoot(int a)
{
	// Write your code here.

	long long low= 1 ,high = a;
	long long ans= 1;
	while(low<=high){
		int mid = (low+high)/2;
		if((mid*mid)<=a){
			ans= mid;
			low=mid+1;
		}
		else{
			high=mid-1;
		}
	}
	return high;
}
